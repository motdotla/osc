<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h1>Explore Projects</h1>

      <ul>
        <% @projects.each do |project | %>
          <li>
            <%= link_to project.url, project_path(url: project.url) %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<br/>
<br/>

<%= render 'shared/add_your_project_jumbo' %>
